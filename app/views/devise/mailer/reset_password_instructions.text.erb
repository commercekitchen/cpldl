Hello <%= @resource.email %>!

Someone has requested a link to change your <%= @resource&.organization&.name %> Digital Learn account password.

You can do this through the link below:

Change my Password: <%= edit_password_url(@resource, reset_password_token: @token, subdomain: (@resource&.organization&.subdomain || 'www')) %>

If you did not request a password reset, please ignore this email.

Your password won't change until you access the link above and create a new one.

Make sure you are using the most recent password reset link. Only the latest reset password email is valid.
