<p><%= t('confirmation_email.welcome') %> <%= @email %>!</p>

<p><%= t('confirmation_email.confirm_your_account_through_link') %></p>

<p><%= link_to "#{t('confirmation_email.confirm_my_account')}", confirmation_url(@resource, confirmation_token: @token, subdomain: @resource.organization.subdomain) %></p>
