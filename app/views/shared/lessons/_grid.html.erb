<div class="container">
<% course.lessons.published.each do |l| %>
  <%= render partial: "shared/lessons/lesson_block",
    locals: { course: course, lesson: l, completed: lessons_completed(course).include?(l.id) }%>
<% end %>
</div>
