<header class="header-logged-out">
  <div class="container">
    <div class="language-links">
      <small><%= link_to "English", "#" %> | <%= link_to "Espa&ntilde;ol".html_safe, "#" %></small>
    </div>
    <%= link_to root_url(subdomain: request.subdomain) do %>
      <div>
        <% case request.subdomain %>
        <% when "admin" %>
          <%= render "shared/logo_white" %>
        <% else %>
          <%= render "shared/logo_white" %>
          <% if content_for?(:callout) %>
            <%= yield :callout %>
          <% else %>
            <h2>Connect to the Digital World</h2>
          <% end %>
        <% end %>
      </div>
    <% end %>
    <button class: "btn" >
      <%= link_to "Sign Up / Log In", login_path, class: "btn-signup-link" %>
    </button>  
  </div>
</header>
