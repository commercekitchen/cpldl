<h1 class="slogan">
  <% if user_signed_in? %>
    <%= t('logged_in_user.hi') %> <%= user_display_name %><%= "(ADMIN)" if current_user.admin? %>!
    <br>
  <% end %>
  <%= raw(CGI.unescape_html(i18n_with_default("home.#{current_organization.subdomain}.custom_banner_greeting"))) %>
</h1>
