<% if Rails.env.production? && !org_admin? %>
  <% if current_organization.gtm_id.present? %>
    <%= render "shared/analytics/gtm" %>
  <% else %>
    <%= render "shared/analytics/ga_#{current_organization.subdomain}" %>
    <%= render "shared/analytics/ga_event_tracking"%>
  <% end %>
<% end %>