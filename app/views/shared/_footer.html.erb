<footer class="footer-primary <%= color_class %>">
  <div class="container">
    <div class="row">
      <div class="threecol">
        <ul class="cms_nav">
          <strong>LEARN MORE</strong>
          <% if @footer_pages %>
            <% @footer_pages.each do |page| %>
              <li><%= link_to page.title, cms_page_path(page) %></li>
            <% end %>
          <% end %>
        </ul>
      </div>
      <%= render "shared/logo_footer" %>
    </div>

    <div class="field_of_dreams">
      <%= hidden_field_tag :user_ga_id, session[:user_ga_id] %>
    </div>

</footer>
