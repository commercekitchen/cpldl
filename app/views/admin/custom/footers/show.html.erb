<% content_for(:callout) do %>
  <h1>Add a custom logo to your footer to appear next to the DigitalLearn logo.</h1>
<% end %>

<%= form_for @organization, url: admin_custom_footers_path, html: { multipart: true } do |f| %>
  <fieldset>
    <%= f.label :footer_logo_link, class: 'text-color' do %>
      Footer Logo Link<span class="required"></span>
    <% end %>
    <%= f.text_field :footer_logo_link, class: "bottom-margin" %>
  </fieldset>
  <fieldset>
    <%= f.label :footer_logo, class: 'text-color' do %>
      Footer logo<span class="required"></span>
    <% end %>
    <%= f.file_field :footer_logo, class: "upload-field" %>
  </fieldset>
  
  <%= f.submit t('common.submit'), class: "button-color" %>
<% end %>
