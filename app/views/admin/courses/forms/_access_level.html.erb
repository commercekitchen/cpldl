<fieldset class='access-level'>
  <%= f.label :access_level do %>
    Access Level
  <% end %>
  <% options = [['Everyone', 'everyone'], ['Authenticated Users', 'authenticated_users']]%>
  <%= f.select(:access_level, options_for_select(options, @course.access_level) ) %>
</fieldset>
